# python-pdf-split-pages
Simple script to slip one multipage PDF file in many PDF's (one per page)
The output PDF has the same name plus page number and is saved at same directory as the input file.

# Requeriments
	PyPDF2

		'''
		pip install PyPDF2
		'''


